server:
  port: 8681
spring:
  application:
    name: base-admin
  cloud:
    nacos:
      server-addr: 192.168.243.33:8848,192.168.243.34:8848
  #数据库配置
  datasource:
    url: jdbc:mysql://192.168.243.20:9097/sa_project?useUnicode=true&useSSL=false&characterEncoding=utf-8
    username: root
    password: 'b#12345678'
    # 使用druid数据源
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations:
    - classpath:dao/*.xml
    - classpath*:com/**/mapper/*.xml
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always
logging:
  file:
    path: admin-client.log #添加开启admin的日志监控
